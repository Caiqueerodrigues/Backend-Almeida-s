server:
  port: 3003 
  error: 
    include-stacktrace: never

upload:
  dir:
    photos: "src/main/resources/photos/"
    photosUsers: "src/main/resources/users/"

backend:
  api: "${API_BACKEND:http://192.168.1.17:3003/}"
frontEnd:
  api: "${API_FRONTEND:http://localhost:3000/}"

security:
  token:
    secret: "${JWT_SECRET:ASDasdhjknfghjn123132lkdfasd}"

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DATABASE_HOST:localhost}:3306/${DATABASE_DATABASE:Almeidas_Cortes}?createDatabaseIfNotExist=true&serverTimezone=GMT-3&autoReconnect=true
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:1234}
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  hikari:
      maxLifetime: 900000  # 15 minutos
      idleTimeout: 600000  # 10 minutos
      validationTimeout: 3000  # 3 segundos
      connectionTimeout: 30000
      connection-test-query: "SELECT 1"
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    ddl-auto: none
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

